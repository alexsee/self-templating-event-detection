from setuptools import find_packages
from setuptools import setup

setup(name='eventlog_inferrence',
      version='1.0.0',
      description='',
      long_description='',
      license='GPL-3.0',
      requires=[],
      packages=find_packages())